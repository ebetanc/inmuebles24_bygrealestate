[Unit]
Description=Run Inmobiliaria24 scraper every 15 minutes, 24/7

[Timer]
# Capture always runs; only notification routing changes during the night window.
OnCalendar=*-*-* *:0/15:00 America/Mexico_City
RandomizedDelaySec=5
Persistent=true

[Install]
WantedBy=timers.target
